# Copy to client/.env. Only VITE_-prefixed variables reach the browser bundle.
# Never put a secret in here — everything in this file ships to every user.

VITE_API_BASE_URL=http://localhost:4000/api/v1
